<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
   <h1>Edit Employee</h1>
 <!-- <ul>  
  
        <li> <%= employee.name %></li>
        <li> <%= employee.gender %></li>
        <li> <%= employee.department %></li>
        <li> <%= employee.email %></li>
        <li> <%= employee.basicSalary %></li> 
        <li> <%= employee.startDate %></li>    

 
  </ul>  -->
  
<form action="/api/user/update/<%= employee.id %>" method="POST">

   <input type="text" name="name" value="<%= employee.name %>">

   <input type="text" name="gender" value="<%= employee.gender %>">

   <input type="text" name="department" value="<%= employee.department %>">

   <input type="email" name="email" value="<%= employee.email %>">

   <input type="number" name="basicSalary" value="<%= employee.basicSalary %>">

   <input type="text" name="startDate" value="<%= employee.startDate %>">

   <button type="submit">Update</button>

</form>
</body>
</html>